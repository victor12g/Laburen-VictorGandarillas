{
  "name": "mcp-clothes-agent",
  "version": "1.0.0",
  "description": "MCP Server for the Laburen Challenge",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18",
    "wrangler": "^3.0.0"
  }
}
